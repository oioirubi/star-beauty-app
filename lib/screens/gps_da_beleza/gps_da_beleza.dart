import 'package:flutter/material.dart';

class GpsDaBeleza extends StatelessWidget {
  const GpsDaBeleza({super.key, required String userType});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bem-vindo ao Starflix!'),
    );
  }
}
