import 'package:flutter/material.dart';

class Starflix extends StatelessWidget {
  const Starflix({super.key, required String userType});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bem-vindo ao Starflix!'),
    );
  }
}
